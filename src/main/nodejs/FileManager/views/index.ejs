<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>文件下载组件</title>
    <style>
        html,
        body {
            margin: 0;
            padding: 0;
            height: 110px;
        }

        /* 组件容器样式 */
        .file-widget {
            font-family: 'Arial', sans-serif;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            height: 100%;
            box-sizing: border-box;
        }

        /* 文件信息样式 */
        .file-info {
            padding: 10px;
            background-color: #f5f5f5;
            height: 100%;
        }

        /* 文件名称样式 */
        .file-name {
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 5px;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        /* 文件大小样式 */
        .file-size {
            font-size: 14px;
            color: #666;
            margin-bottom: 10px;
        }

        /* 下载按钮样式 */
        .download-btn {
            display: block;
            padding: 8px 15px;
            background-color: #3498db;
            color: #fff;
            text-align: center;
            text-decoration: none;
            border-radius: 3px;
            transition: background-color 0.3s ease;
        }

        /* 鼠标悬停时下载按钮样式 */
        .download-btn:hover {
            background-color: #2980b9;
        }
    </style>
</head>

<body>

    <!-- 文件信息组件 -->
    <div class="file-widget" title="<%= originalFilename %>">
        <div class="file-info">
            <div class="file-name">
                <%= originalFilename %>
            </div>
            <div class="file-size">文件大小: <%= fileSize %>
            </div>
            <a href="<%= downloadUrl %>" class="download-btn">下载</a>
        </div>
    </div>

</body>

</html>
